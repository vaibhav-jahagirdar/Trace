import { ChapterMark, FigureCaption } from "@/components/editorial";

export function ChapterProcess() {
  const volume = ["Template", "Keyword", "Checkbox", "Next"];
  const correctness = ["Context", "Trade-offs", "Depth", "Evidence"];
  return <section className="bg-paper px-6 py-24 text-ink md:px-12 md:py-32 lg:px-20"><div className="mx-auto max-w-6xl"><ChapterMark index="06" label="The system, not the people" /><div className="mt-14 grid gap-14 md:grid-cols-12 md:gap-16"><div className="md:col-span-7"><h2 className="max-w-[12ch] font-sans text-[clamp(2.7rem,4vw,3.7rem)] font-normal leading-[.94] tracking-[-.055em]">Nobody here<br />is the villain.</h2><p className="mt-8 max-w-[46ch] text-base leading-[1.7] text-olive">Recruiters are measured on throughput, so they work with templates, keywords and checkboxes. Engineering managers are measured on correctness, and receive resumes with context stripped out. Both sides are doing their job correctly. The handoff between them is what fails.</p></div><figure className="md:col-span-5"><div className="border border-forest/12 bg-warm p-6"><div className="grid grid-cols-2 gap-6">{[["Volume", volume], ["Correctness", correctness]].map(([title, items]) => <div key={title as string}><span className="label-index text-forest">Optimised for</span><p className="mt-3 font-sans text-sm">{title as string}</p><div className="mt-4 space-y-1">{(items as string[]).map((item) => <div key={item} className="border border-forest/10 px-2 py-1 font-mono text-[10px] text-olive">{item}</div>)}</div></div>)}</div><div className="mt-6 border-t border-forest/15 pt-4"><p className="font-mono text-[11px] leading-relaxed text-forest">handoff → context loss → decision made on what survived</p></div></div><FigureCaption id="Fig. 6.1">Two correct objectives, one lossy interface between them.</FigureCaption></figure></div></div></section>;
}
